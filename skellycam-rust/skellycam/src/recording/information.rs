//! RecordingInformation: recording name, directory paths, UUID,
//! folder schema (videos, timestamps, audio). Mirror of Python's RecordingInfo.

pub struct RecordingInformation {}

impl RecordingInformation {
    pub fn new() -> Self {
        Self {}
    }
}
