//! CameraGroup: owns a set of synchronized cameras, their threads,
//! channels, and the orchestrator. Created and managed by CameraGroupManager.

pub struct CameraGroup {}

impl CameraGroup {
    pub fn new() -> Self {
        Self {}
    }
}
