---
title: التثبيت
sidebar_position: 1
---
import { AiGeneratedBanner } from '@freemocap/skellydocs';

<AiGeneratedBanner humanCurated humanNotes="Maybe some screenshots or something? more info about the installer and the difference between the app and the server_only stuff"/>

# التثبيت

## تحميل SkellyCam

أسهل طريقة للبداية هي تحميل المثبّت الجاهز لنظام التشغيل تبعك من [صفحة التحميل](/download).

المنصات المتوفرة:
- **Windows** — مثبّت `.exe`
- **macOS** — `.dmg` (Intel و Apple Silicon)
- **Linux** — `.AppImage` أو `.deb`

المثبّت مستقل بذاته — بيجمع سيرفر SkellyCam وواجهة المستخدم مع بعض، فما بتحتاج تثبّت Python أو Node.js أو أي تبعيات تانية.

### ملاحظات لـ Linux

- ممكن تحتاج تخلّي ملف `.AppImage` قابل للتنفيذ: `chmod +x SkellyCam-*.AppImage`
- تسجيل الصوت بيحتاج PortAudio: `sudo apt install portaudio19-dev`

## التثبيت من الكود المصدري

إذا كنت مطوّر أو مساهم وبدك تشغّل من الكود المصدري، شوف قسم [التطوير](/docs/development/) للتعليمات الكاملة.
