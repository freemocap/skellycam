---
title: Logging
sidebar_position: 5
---
<AiGeneratedBanner humanCurated humanNotes="Should add some info section like talking about how logs are super important for deeply complex tools like skellycam and freemocap. Obervability is key to understanding what is happening under the hood! Also should note that the UI is designed to mirror the backend logs, but that obviously only works if the server is in a good state (which is when you'd want to see the logs!) for max observability, set 'auto-connect' to false and run the server in its own terminal. Also note that the Logs terminal window in the UI has a copy-to-clipboard and save to disk, which will be helpful when asking for help on the Discord server or in github issues (and then link to the community page) " />


# Logging

SkellyCam uses [skellylogs](https://github.com/freemocap/skellylogs) for logging, which provides custom log levels and WebSocket log forwarding. The log level is set in `skellycam/__init__.py`:

```python
LOG_LEVEL = LogLevels.TRACE  # Change to LogLevels.INFO for less verbose output
```

Logs at `TRACE` level and above are forwarded to connected WebSocket clients for display in the UI's log terminal.

Log files are saved to `~/skellycam_data/logs_info_and_settings/logs/`.
