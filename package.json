{
  "name": "skellycam-tauri-nuxt-app",
  "private": true,
  "type": "module",
  "dependencies": {
    "@mdi/font": "^7.4.47",
    "@tauri-apps/api": "^1.5.3",
    "execa": "^8.0.1",
    "fluent-ffmpeg": "^2.1.3",
    "node-webcam": "^0.8.2",
    "usb": "^2.13.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.5.11",
    "cross-env": "^7.0.3",
    "shx": "^0.3.4",
    "vite-plugin-vuetify": "^2.0.3",
    "vuetify": "^3.5.16"
  },
  "scripts": {
    "dev": "npm run tauri dev",
    "tauri": "tauri",
    "tauri-dev": "npm run tauri dev",
    "build": "tauri build",
    "tauri-build": "tauri build",
    "nuxt-dev": "cd skellycam-ui && npm run dev",
    "install-nuxt": "cd skellycam-ui && npm install",
    "python-dev": "poetry shell && poetry run skellycam",
    "build-python-sidecar": "npm run install-python && poetry run pyinstaller",
    "install-python": "poetry shell && poetry update",
    "postinstall": "npm run install-nuxt"
  }
}
